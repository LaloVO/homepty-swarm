[project]
name = "homepty-swarm-runtime"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
license = "AGPL-3.0-only"
dependencies = [
  "flask==3.1.3", "gunicorn>=23", "pydantic==2.12.5", "rfc8785==0.1.4",
  "psycopg[binary]>=3.2.10", "boto3>=1.40", "jsonschema>=4.25", "httpx>=0.28.1",
  "prometheus-client>=0.23",
]
[dependency-groups]
dev = ["pytest>=9.0.3", "moto[s3,server]>=5.1"]
[tool.uv]
package = false
